#!/usr/bin/env bun

/**
 * interface Action {
 *   type: 'add' | 'list'
 *   action: 'placeMarketOrder' | 'placeLimitOrder' | 'cancelOrder'
 *   params: Record<string, any>
 *   result: 'success' | 'failed'
 *   reason: string
 *   timestamp: string
 * }
 * interface Memo {
 *   date: string
 *   content: string
 *   actions: Action[]
 * }
 * add_memo(memo: Memo)
 * list_memo(last_n: number = 100)
 */
